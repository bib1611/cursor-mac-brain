---
type: agent-receipt
date: 2026-06-29 10:24 CDT
agent: Hermes
owner: Adam
status: completed
surface: Hermes Desktop / Codex / stewardship-room-app
---

# Hermes - Codex 5.5 switch and build verification

## Request

Adam asked: `switch to codex 5.5 and verify this build`.

## Model switch

Hermes default config was updated:

```text
model.provider = openai-codex
model.default = gpt-5.5
```

Verified from `/Users/adamjohnsson/.hermes/config.yaml` without printing secrets.

## Codex 5.5 smoke test

Command used:

```bash
/Users/adamjohnsson/homebrew/bin/codex exec --skip-git-repo-check --dangerously-bypass-approvals-and-sandbox -m gpt-5.5 -C /Users/adamjohnsson -o <tmp> 'Reply exactly: CODEX_55_READY'
```

Result:

```text
exit_code=0
stdout_file=CODEX_55_READY
stderr showed model: gpt-5.5 provider: openai
```

Non-blocking stderr observations:

- Linear MCP OAuth token invalid/missing.
- `codex_memories_write` reported missing `jobs` table.

The Codex 5.5 model call itself succeeded.

## Hermes 5.5 smoke test

Command used:

```bash
/Users/adamjohnsson/.agents/bin/dh-with-env hermes chat -q 'Reply exactly: HERMES_CODEX_55_READY' --provider openai-codex -m gpt-5.5 -t '' -Q
```

Result:

```text
HERMES_CODEX_55_READY
```

Hermes warned:

```text
Warning: Unknown toolsets: messaging, moa
```

This does not block the model switch. It means those toolset names are not available in that CLI context.

## Jarvis/Linq check

`/Users/adamjohnsson/.deadhidden-os/imessage-brain/jarvis_linq.py` already used Codex GPT-5.5:

```text
BASE_ARGS includes -m gpt-5.5
CODEX_PATH /Users/adamjohnsson/homebrew/bin/codex
```

Control bridge verification:

- Jarvis/Linq running as PID `96242`.
- `dh-control status --text` works.
- Normal non-command text still falls back to Jarvis's regular brain.

## Build verification

Root workspace test command:

```bash
npm run test
```

Result:

```text
Error: no test specified
exit_code=1
```

This is a package-script stub in `/Users/adamjohnsson/Hermes-Workspace/package.json`, not a build failure in the app.

App build verified at:

```text
/Users/adamjohnsson/Hermes-Workspace/stewardship-room-app
```

Commands:

```bash
npm ci
npm run build
```

Result:

```text
added 154 packages
vite v8.0.10 building client environment for production...
✓ 20 modules transformed.
dist/index.html                   0.47 kB │ gzip:  0.30 kB
dist/assets/index-s86qKApW.css    8.29 kB │ gzip:  2.58 kB
dist/assets/index-EL6mcP6V.js   218.76 kB │ gzip: 67.16 kB
✓ built in 322ms
```

## Audit note

`npm audit` reports 3 vulnerabilities in `stewardship-room-app` dependencies:

- low: `@babel/core`
- moderate: `brace-expansion`
- high: `vite`

Fixes are available, but not applied in this run.

## Files touched

- `/Users/adamjohnsson/.hermes/config.yaml`
- `/Users/adamjohnsson/Hermes-Workspace/stewardship-room-app/node_modules/`
- package lock/state may have been refreshed by `npm ci` if missing local install state.

## Boundary

No external publishing, sending, refunds, deployment, or account changes were performed.
